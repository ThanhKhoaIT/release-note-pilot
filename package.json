{
  "name": "release-note-pilot",
  "version": "0.1.0",
  "private": true,
  "description": "Generate business-friendly release notes from commits/PRs and post them to Slack.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify --license licenses.txt",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.1",
    "eslint": "^9.9.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0",
    "undici": "^6.19.0",
    "vitest": "^3.2.7"
  }
}
